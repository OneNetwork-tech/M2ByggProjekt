# M2 Platform — CRM directory hardening

<FilesMatch "^(migrate-to-mysql|cron-backup|cron-invoice-reminders|config|config\.local)\.php$">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Deny from all
  </IfModule>
</FilesMatch>
